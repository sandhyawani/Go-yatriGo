                              <MessageSquare className="w-[22px] h-[22px] sm                        {/* Expanded comments */}
                        <AnimatePresence>
                          {activeCommentPost === post._id && (
                            <motion.div
                              initial={{ opacity: 0, height: 0 }}
                              animate={{ opacity: 1, height: "auto" }}
                              exit={{ opacity: 0, height: 0 }}
                              className="overflow-hidden border-t border-slate-50 pt-3 mt-2"
                            >
                              <div className="bg-slate-50 border border-slate-100 rounded-2xl p-3">
                                <div className="space-y-3 max-h-[300px] overflow-y-auto [&::-webkit-scrollbar]:hidden scrollbar-hide pr-1">
                                  <AnimatePresence initial={false}>
                                    {post.comments?.map((comment) => (
                                      <motion.div
                                        key={comment._id}
                                        initial={{ opacity: 0, y: 10 }}
                                        animate={{ opacity: 1, y: 0 }}
                                        className="flex gap-2.5 items-start group relative"
                                      >
                                        <Link to={`/profile/${comment.userId?._id || comment.userId}`} className="shrink-0 mt-0.5">
                                          <img
                                            src={getAvatarUrl(comment.userId?.pic, comment.userPic, comment.userName)}
                                            alt={comment.userName}
                                            className="w-6 h-6 rounded-full object-cover border border-slate-200"
                                            onError={(e) => handleAvatarError(e, comment.userName)}
                                          />
                                        </Link>
                                        <div className="flex-1 min-w-0">
                                          <div className="flex items-baseline gap-1.5">
                                            <Link to={`/profile/${comment.userId?._id || comment.userId}`} className="text-[11px] font-bold text-[#111827] hover:text-[#6C4DF6]">
                                              {comment.userName}
                                            </Link>
                                            <span className="text-[9px] text-slate-400">{moment(comment.createdAt).fromNow()}</span>
                                          </div>
                                          <p className="text-[11px] text-slate-700 leading-snug break-words mt-0.5">{comment.text}</p>
                                        </div>
                                        {((comment.userId?._id || comment.userId)?.toString() === myUserId || isCreator) && (
                                          <button
                                            onClick={() => handleDeleteComment(post._id, comment._id)}
                                            aria-label="Delete comment"
                                            className="opacity-0 group-hover:opacity-100 p-1 text-slate-400 hover:text-rose-500 transition-opacity absolute right-0 top-0"
                                          >
                                            <Trash2 className="w-3 h-3" />
                                          </button>
                                        )}
                                      </motion.div>
                                    ))}
                                  </AnimatePresence>
                                </div>
                              </div>
                            </motion.div>
                          )}
                        </AnimatePresence>

                        {/* Comment input */}
                        <form
                          onSubmit={(e) => handleCommentSubmit(e, post._id)}
                          className="flex items-center gap-2.5 pt-3 mt-3 border-t border-[#94a3b81f]"
                        >
                          <img
                            src={getAvatarUrl(user?.pic, null, user?.name)}
                            alt="Your avatar"
                            className="w-7 h-7 rounded-full object-cover shrink-0"
                            onError={(e) => handleAvatarError(e, user?.name)}
                          />
                          <div className="flex-1 relative flex items-center">
                            <input
                              type="text"
                              value={commentText[post._id] || ""}
                              onChange={(e) =>
                                setCommentText(prev => ({ ...prev, [post._id]: e.target.value }))
                              }
                              placeholder="Add a comment..."
                              maxLength={500}
                              className="w-full bg-slate-50 border border-slate-100 rounded-full pl-4 pr-10 py-2 text-xs text-[#111827] placeholder:text-slate-400 outline-none focus:border-[#6C4DF6]/50 focus:bg-white focus:shadow-sm transition-all"
                            />
                            <button
                              type="submit"
                              disabled={isSubmittingComment[post._id] || !commentText[post._id]?.trim()}
                              aria-label="Post comment"
                              className="absolute right-1 p-1.5 bg-gradient-to-r from-violet-500 to-[#6C4DF6] text-white rounded-full active:scale-90 transition-all disabled:opacity-0 disabled:scale-75 disabled:pointer-events-none shadow-sm"
                            >
                              {isSubmittingComment[post._id]
                                ? <Loader2 className="w-3 h-3 animate-spin" />
                                : <Send className="w-3 h-3 -ml-0.5 mt-0.5" />}
                            </button>
                          </div>
                        </form>>
                                        {((comment.userId?._id || comment.userId)?.toString() === myUserId || isCreator) && (
                                          <button
                                            onClick={() => handleDeleteComment(post._id, comment._id)}
                                            aria-label="Delete comment"
                                            className="opacity-0 group-hover:opacity-100 p-1 text-slate-400 hover:text-rose-500 transition-opacity"
                                          >
                                            <Trash2 className="w-3.5 h-3.5" />
                                          </button>
                                        )}
                                      </div>
                                      <p className="text-xs text-slate-600 mt-0.5 leading-normal">{comment.text}</p>
                                    </div>
                                  </div>
                                ))}
                              </div>
                            </motion.div>
                          )}
                        </AnimatePresence>

                        {/* Comment input */}
                        <form
                          onSubmit={(e) => handleCommentSubmit(e, post._id)}
                          className="flex items-center gap-3 pt-3 mt-3 border-t border-[#94a3b81f]"
                        >
                          <img
                            src={getAvatarUrl(user?.pic, null, user?.name)}
                            alt="Your avatar"
                            className="w-7 h-7 rounded-full object-cover border border-slate-100 shrink-0"
                            onError={(e) => handleAvatarError(e, user?.name)}
                          />
                          <input
                            type="text"
                            value={commentText[post._id] || ""}
                            onChange={(e) =>
                              setCommentText(prev => ({ ...prev, [post._id]: e.target.value }))
                            }
                            placeholder="Add a comment…"
                            maxLength={500}
                            className="flex-1 bg-slate-50 border border-slate-100 rounded-xl px-4 py-2.5 text-xs text-[#111827] placeholder:text-slate-400 outline-none focus:border-[#6C4DF6] focus:bg-white transition-all"
                          />
                          <button
                            type="submit"
                            disabled={isSubmittingComment[post._id]}
                            aria-label="Post comment"
                            className="p-2.5 bg-[#6C4DF6] hover:bg-[#5b3ee0] text-white rounded-xl active:scale-90 hover:-translate-y-0.5 transition-all disabled:opacity-50"
                          >
                            {isSubmittingComment[post._id]
                              ? <Loader2 className="w-3.5 h-3.5 animate-spin" />
                              : <Send className="w-3.5 h-3.5" />}
                          </button>
                        </form>
                      </div>
                    </motion.div>
                  );
                })}
              </div>
            )}
          </div>

          {/* ── Right sidebar ── */}
          <RightSidebar
            user={user}
            suggestions={suggestions}
            nearbyTrips={nearbyTrips}
            handleFollowToggle={handleFollowToggle}
            followLoadingMap={followLoadingMap}
          />
